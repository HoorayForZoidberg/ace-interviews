  <!-- profile card -->
  <% if current_user != @user %>
    <%= render "users/profile", user: @user, meetings: @meetings %>
  <% else %>
    <%= render "users/me", user: @user, meetings: @meetings %>
  <% end %>
